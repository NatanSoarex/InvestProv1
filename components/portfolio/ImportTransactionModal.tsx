
import React from 'react';

// Componente descontinuado/removido por solicitação do usuário.
// Mantendo estrutura vazia para evitar quebras de build caso existam imports residuais.
export const ImportTransactionModal: React.FC<{ onClose: () => void }> = ({ onClose }) => {
  return null;
};
