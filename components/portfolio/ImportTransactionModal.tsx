
import React from 'react';

// Component descontinuado/removido.
// Mantido vazio para evitar erros de build caso ainda haja imports pendentes.
export const ImportTransactionModal: React.FC<{ onClose: () => void }> = ({ onClose }) => {
  return null;
};
